<h1>Listing tasks</h1>

<table>
  <tr>
    <th>Place</th>
    <th>S Name</th>
    <th>Title</th>
    <th>Moveto</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @tasks.each do |task| %>
  <tr>
    <td><%= task.station %></td>
    <td><%= Station.find(task.station).station_name.to_s %></td>
    <td><%= task.title %></td>
    <td><%= task.moveto %></td>
    <td><%= link_to 'Destroy', task, confirm: 'Are you sure?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Task', new_task_path %>
